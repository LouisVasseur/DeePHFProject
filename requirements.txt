# ── DeePHFProject — Python dependencies ──────────────────────────────────
#
# Install everything with:
#   pip install -r requirements.txt
#
# Tested on Python 3.10 / 3.11, Linux only (PySCF requirement).
# For the web interface specifically, see app/backend/requirements.txt.
# ── Quantum chemistry ─────────────────────────────────────────────────────
pyscf>=2.5                # Hartree-Fock, MP2, density matrix, cubegen for orbitals
# ── Atomic descriptors ────────────────────────────────────────────────────
dscribe>=2.1              # SOAP descriptors
# ── Machine learning ──────────────────────────────────────────────────────
torch>=2.0
scikit-learn>=1.3         # Ridge regression pre-fitting, z-score normalisation
# ── Data & numerics ───────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.11
h5py>=3.9                 # MOB-ML / QM7-X dataset loading
# ── Cheminformatics ───────────────────────────────────────────────────────
rdkit>=2023.3             # SMILES → 3D XYZ conversion (ETKDG + MMFF)
# ── Visualisation (optional, for plot_results.py) ─────────────────────────
matplotlib>=3.7
pandas>=2.0
# ── Streamlit interface (app_streamlit/app.py) ───────────────────────────
streamlit>=1.35           # Streamlit itself
streamlit-ketcher>=0.0.1  # Drawing input → SMILES via the Ketcher editor
py3Dmol>=2.0              # 3D molecule + MO isosurface viewer (3Dmol.js wrapper)
# ── Web interface (used inside Docker — listed here for completeness) ──────
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.0             # SmilesRequest model for /predict_smiles
